[project]
name = "agentic-qa-safety-manager"
version = "1.0.0"
description = "Governed multi-agent QA and safety assurance reference architecture"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
